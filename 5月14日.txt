5月14日日志
上午：
1.学习多通道ADC-DMA开启，实现了多通道的ADC采样。在开启了DMA的中断后，就需要关闭ADC的全局中断了，因为DMA的中断回调函数最终也会调制到
ADC的中断回调函数。为了防止重复进入，就需要关闭一个。因为DMA搬运完，是最终的结果，就需要留下这个；
下午：
1.力扣刷题
	这个一个题的题意是给定一个数组，找到这个数组中唯一出现过两次的数。这个题要求空间复杂度为常数，因此就很难解决。
	我不管那么多了，直接使用哈希表查询出来了，嵌入式管那么多干嘛！
2.更新了四通道ADC-DMA搬运的程序，主要的流程如下：
	定时器更新事件触发
	ADC进行采样
	扫描转换多通道
	DMA搬运，搬运完成产生中断
	中断发生信号给ADC采样任务， vTaskNotisfyGiveFromISR()
	ADC采样任务从缓冲区读取数据
晚上：
1.完成LCD的屏幕的驱动移植
2.复习了c语言关键字：const、static、valotile、extern
明日计划：
1.刷题
2.完成adc的学习任务
3.八股学习